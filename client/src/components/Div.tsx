import styled from "@emotion/styled";
import {
  space,
  color,
  typography,
  layout,
  flexbox,
  border,
  position,
} from "styled-system";

const Div = styled("div")(
  space,
  color,
  typography,
  layout,
  flexbox,
  border,
  position
);

export default Div;
